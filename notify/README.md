# notify

On-screen notification cards. Built and in use.

`bin/notify --help` is the CLI contract. `layout.lua`, `card.lua` and `gesture.lua` carry their own documentation, and every limit is in `configConsts.notify`.

Open work: [issues labelled `notify`](https://github.com/matthewfallshaw/hammerspoon-config/labels/notify).
